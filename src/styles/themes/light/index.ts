import { DefaultTheme } from 'styled-components';
import { lightTheme as componentLibLightTheme } from '@diogop_96/portfolio-component-library';

import { colors } from './colors';

export const lightTheme: DefaultTheme = {
  ...componentLibLightTheme,
  colors
};
